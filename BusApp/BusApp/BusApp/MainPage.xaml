<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="BusApp.MainPage">

    <StackLayout>
        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <Label Text="On duty" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>

        <maps:Map x:Name="myMap" IsShowingUser="True"/>
        <!--<maps:Map x:Name="myMap"
                  ItemsSource="{Binding }">
            <maps:Map.ItemTemplate>
                <DataTemplate>
                    <maps:Pin Position="{Binding Position}"
                              Address="{Binding Address}"
                              Label="{Binding Description}" />
                </DataTemplate>
            </maps:Map.ItemTemplate>
        </maps:Map>-->

        <Button Text="Start sharing your location" Clicked="getUserCoordinates" />
        <Button Text="Stop sharing your location" Clicked="stopGettingCoordinates" />

        <Button Text="start getting others location"  Clicked="startGettingOthersLocation"/>
        <Button Text="stop getting others location" Clicked="stopGettingOthersLocation"/>

    </StackLayout>

</ContentPage>
