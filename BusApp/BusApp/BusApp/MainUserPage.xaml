<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="BusApp.MainUserPage">
    <StackLayout>
        <Frame BackgroundColor="#2196F3" Padding="10" CornerRadius="0">
            <Label Text="Search Buses" HorizontalTextAlignment="Center" TextColor="White" FontSize="18"/>
        </Frame>

        <Picker x:Name="Buses" SelectedIndex="1" SelectedIndexChanged="Buses_SelectedIndexChanged" SelectedItem="1" />

        <maps:Map x:Name="myMap" IsShowingUser="True"/>
     

        <Button Text="Start track" 
                        Background="green" 
                        HorizontalOptions="Center" 
                        VerticalOptions="Center" 
                        CornerRadius="15" WidthRequest="170"
                        Clicked="startGettingOthersLocation"/>
        <Button Text="Stop Track" 
                        Background="red" 
                        HorizontalOptions="Center" 
                        VerticalOptions="Center" 
                        CornerRadius="15" 
                        WidthRequest="170" 
                        Clicked="stopGettingOthersLocation"
                        />
    </StackLayout>


</ContentPage>
